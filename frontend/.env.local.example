# Required when the frontend must call a backend other than its built-in local default.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Optional server-side API URL. It falls back to NEXT_PUBLIC_API_BASE_URL when omitted.
INTERNAL_API_BASE_URL=http://127.0.0.1:8000
